# global_theme_changer
A React application featuring a global light/dark theme switcher built with the Context API. Includes a ThemeProvider, dynamic UI styling, and a toggle component that updates theme state across the entire app.
